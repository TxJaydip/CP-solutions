#include "bits/stdc++.h"
using namespace std;

#define ll long long int

int main()
{
    ll n, q, t;
    cin >> n >> q;
    ll q1 = q;
    ll i, j, k;
    while (q1--)
    {
        cin >> t;
        if( t == 1 )
        {
            cin >> i >> j >> k;

            

        }
        else
        {
            cin >> i >> j;
        }
    }
    


    return 0;
}